# wsgnay blog

Quartz-powered personal blog and AI notes.

## Local preview

```bash
npm ci
npx quartz build --serve
```

## Deploy

Import this repository into Vercel and use:

- Build command: `npx quartz plugin install && npx quartz build`
- Output directory: `public`
